# power-tools.js
 lib to control you device (shutting down / rebooting / ...)

## getting started
```javascript

    // import powerTools
    import powerTools from '../index.js';

    // sleep
    powerTools.sleep();

    // shutdown
    powerTools.shutdown();

    // reboot
    powertools.reboot();

```